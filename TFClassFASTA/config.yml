# Database settings.
database:
    # the name of the JDBC driver, mysql in our case
    driverClass: org.sqlite.JDBC
    # the username
    user: tfadmin
    # the password
    password: test123456
    # the JDBC URL
    url: jdbc:sqlite:fastadb.sqlite

logging:
  level: INFO
  loggers:
    BioinformatikGoettingen: DEBUG
